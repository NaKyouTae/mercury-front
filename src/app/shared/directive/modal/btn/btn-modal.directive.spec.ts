import { BtnModalDirective } from './btn-modal.directive';

describe('BtnModalDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnModalDirective();
    expect(directive).toBeTruthy();
  });
});
