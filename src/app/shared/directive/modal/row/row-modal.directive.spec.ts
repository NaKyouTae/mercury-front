import { RowModalDirective } from './row-modal.directive';

describe('RowModalDirective', () => {
  it('should create an instance', () => {
    const directive = new RowModalDirective();
    expect(directive).toBeTruthy();
  });
});
