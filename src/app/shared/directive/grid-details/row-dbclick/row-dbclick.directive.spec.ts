import { RowDbclickDirective } from './row-dbclick.directive';

describe('RowDbclickDirective', () => {
  it('should create an instance', () => {
    const directive = new RowDbclickDirective();
    expect(directive).toBeTruthy();
  });
});
