import { SelectedListDirective } from './selected-list.directive';

describe('SelectedListDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedListDirective();
    expect(directive).toBeTruthy();
  });
});
